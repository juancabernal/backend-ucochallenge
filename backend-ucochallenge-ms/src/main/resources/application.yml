spring:
  application:
    name: UcoChallenge

  datasource:
    # 🔹 URL y driver necesarios para que Spring Boot inicialice el DataSource correctamente
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d3n8p524d50c73f4am9g-a.oregon-postgres.render.com:5432/software2terraxs}
    driver-class-name: ${SPRING_DATASOURCE_DRIVER:org.postgresql.Driver}

server:
  port: 8083

services:
  message:
    base-url: http://localhost:8082/api/v1/messages
  parameters:
    base-url: http://localhost:8083/api/v1/parameters

azure:
  keyvault:
    url: https://uco-vault-secrets.vault.azure.net/

# 🔹 Definición de secretos para ser obtenidos desde Azure Key Vault o variables de entorno
secrets:
  db-username: ${DB_USERNAME}
  db-password: ${DB_PASSWORD}

logging:
  level:
    root: INFO
    com.azure.identity: INFO
    co.edu.uco.ucochallenge.infrastructure.secondary.secret: DEBUG
